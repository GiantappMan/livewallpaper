## 移除托盘"退出 GitHub 登录"菜单项

背景:该项是提交 5254754d 加入的,用于切换 GitHub 账号(清 github.com 会话 Cookie),但无条件始终显示,用户认为托盘菜单杂乱。5254754d 之后没有其它提交改过 tray.rs,可整体恢复。

### 改动(仅 src-tauri/src/tray.rs,恢复到 5254754d^ 的状态)

1. 删除 `TrayTexts.github_logout` 字段及 zh/ru/es/en 四处文案("退出 GitHub 登录"等)。
2. 删除 `github-logout` 菜单项创建,菜单恢复为 `Menu::with_items(app, &[&about, &open, &settings, &quit])`(去掉分隔线,与原版一致)。
3. 删除 `on_menu_event` 里 `"github-logout"` 事件处理分支。

执行方式:`git checkout 5254754d^ -- src-tauri/src/tray.rs`(该文件后续无其它改动,等效于手动还原)。

### 保留不动

- `lib.rs` 的 `clear_github_session`(lib.rs:578)——控制管道(control.rs)仍调用它,不会有死代码。
- "GitHub 登录每次强制重新认证"的核心修复逻辑(handle_new_window_request)。

### 验证

- `cd src-tauri && no_proxy='*' cargo check` 编译通过。
- 不自动提交;用户确认后可自行或由我提交。